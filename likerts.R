#Levels

agreement <- c("Strongly agree", "Agree",
               "Slightly agree", "Slightly disagree",
               "Disagree", "Strongly disagree")

agreementNA <- c("Strongly agree", "Agree", "Slightly agree", 
                 "Slightly disagree", "Disagree", "Strongly disagree", 
                 "Don't know/ NA")

class <- c("FY/freshman", "Sophomore", "Junior", "Senior")

dukeagree <- c("Strongly agree", "Agree", "Neither", "Disagree", "Strongly disagree")

frequency <- c("Not at all", "Several days", "Half the days", "Over half the days", 
               "Nearly every day")

gender <- c("Male", "Female", "Other")

gpa <- c("A+", "A or A-", "B+", "B", "B-", "C+", "C", "C-", "D or lower")

helpful <- c("Very helpful", "Helpful", "Sometimes helpful", "Seldom helpful",
             "Never helpful", "N/A")

housing <- c("First-year student", "Culture-specific", "Special academic", 
             "Other special interest", "Residence hall", "Apartment", "Fraternity or sorority",
             "Other residential", "At home with family", "Off-campus fraternity or sorority", "Rented apartment or house", "Other")

likely <- c("Very likely", "Moderately likely", "Slightly likely", "Slightly unlikely", 
            "Moderately unlikely", "Very unlikely")

loans <- c("Less than $10,000","$10,000 - $19,000", "$20,000 - $29,999", 
           "$30,000 - $39,999", "$40,000 or more", "Not sure")

military <- c("None", "ROTC, cadet, midshipman at service academy", "Reserve or National Guard", 
              "Active Duty", "Discharched vet")

often <- c("Very often", "Often", "Sometimes", "Seldom", "Never")

purpose <- c("Have goals", "Defining goals", "No goals")

ra <- c("Yes", "No", "No, never")

race <- c("Hispanic/ Latino", "American Indian/ Alaska Native", "Asian", "Black or AA", 
          "Native Hawaiian or PI", "White", "2 or more")

romantic <- c("In relationship", "Would be in relationship", "Casual relationships only", "Not interested")

sexor <- c("Asexual", "Bisexual", "Gay", "Heterosexual", "Lesbian", "Other")

spirituality <- c("Buddhist", "Catholic", "Protestant", "Christian Other", 
                  "Hindu", "Jewish", "Muslim", "Other", "Multiple", "None", "Unknown")

substance <- c("0", "1", "2", "3-5", "6-9", "10+")

work <- c("0", "1-7", "8-12", "13 or more")

yn <- c("Yes", "No")

ynd <- c("Yes", "No", "Don't know")

ynns <- c("Yes", "No", "Not sure")

ynplan <- c("Yes", "No", "No, but plan to")

